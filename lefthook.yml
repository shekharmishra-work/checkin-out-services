min_version: 2.1.6

pre-commit:
  parallel: true
  jobs:
    - name: ruff check
      run: uv run ruff check .

pre-push:
  parallel: true
  jobs:
    - name: mypy
      run: uv run mypy app tests

    - name: pytest
      run: uv run pytest
